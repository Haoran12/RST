# 开发日志（2026-02-26开始）

## 概览
1. 完成 M0 前后端骨架与规范对齐，打通 `/health` 通路与前端 Shell 显示。
2. 补齐环境变量、脚本与测试骨架，满足一键启动与验收的最低闭环。

## 后端
1. 新增配置加载与项目根目录探测：`backend/app/config.py`。
2. 实现数据目录初始化并接入 lifespan：`backend/app/storage/init_dirs.py`、`backend/app/main.py`。
3. `/health` 路由返回 200 与 `{"status": "ok"}`：`backend/app/routers/health.py`。
4. 增补健康检查测试（ASGITransport）：`backend/tests/test_health.py`。
5. 依赖与工具链规范化：`backend/pyproject.toml`。

## 前端
1. 完成 Vite + Vue3 + TS 基础配置与 alias：`frontend/vite.config.ts`、`frontend/tsconfig.json`、`frontend/tsconfig.node.json`。
2. 建立路由与应用入口：`frontend/src/router/index.ts`、`frontend/src/main.ts`。
3. 实现 Shell 页面与后端联通状态展示：`frontend/src/views/ChatView.vue`。
4. axios 客户端与健康接口封装：`frontend/src/api/client.ts`、`frontend/src/api/health.ts`。
5. 完成 SCSS 变量/重置/全局样式：`frontend/src/styles/variables.scss`、`frontend/src/styles/reset.scss`、`frontend/src/styles/global.scss`。

## 脚本与工程化
1. 新增一键初始化/启动/lint/test 脚本：`scripts/setup.bat`、`scripts/dev.bat`、`scripts/lint.bat`、`scripts/test.bat`。
2. 环境变量模板与忽略规则补齐：`.env.example`、`.gitignore`。

## 测试
。

---

## M1 实现
1. 增加核心模型与 ID 生成：`backend/app/models/__init__.py`、`backend/app/models/api_config.py`、`backend/app/models/session.py`、`backend/app/models/preset.py`。
2. 完成存储层与加密：`backend/app/storage/file_io.py`、`backend/app/storage/encryption.py`、`backend/app/storage/message_store.py`。
3. API 配置服务与路由落地：`backend/app/services/api_config_service.py`、`backend/app/routers/api_configs.py`。
4. Provider 适配与注册：`backend/app/providers/*`。
5. 默认 Preset 初始化并接入启动流程：`backend/app/services/preset_service.py`、`backend/app/main.py`。
6. 前端 API 配置全链路：`frontend/src/types/api-config.ts`、`frontend/src/api/api-configs.ts`、`frontend/src/stores/api-config.ts`、`frontend/src/components/api-config/*`、`frontend/src/App.vue`。
7. 环境与依赖更新：`.env.example`、`.gitignore`、`backend/pyproject.toml`。

## 测试
1. 后端：`uv run pytest` 通过（28 passed，存在 `datetime.utcnow()` deprecation warnings）。
2. 前端：`pnpm test` 通过（3 tests）。
3. 前端：`pnpm type-check` 通过。

## 修正
1. 测试数据改用时区感知 UTC 时间，清除 `datetime.utcnow()` 警告：`backend/tests/test_message_store.py`。
2. 重新执行后端测试：`uv run pytest`（28 passed，无 warnings）。

---
## M2 实现（续）
1. 后端补齐 Session/Preset CRUD 服务、路由与响应模型，加入重命名与引用保护：`backend/app/models/session.py`、`backend/app/models/preset.py`、`backend/app/services/session_service.py`、`backend/app/services/preset_service.py`、`backend/app/services/api_config_service.py`、`backend/app/routers/sessions.py`、`backend/app/routers/presets.py`、`backend/app/routers/api_configs.py`、`backend/app/main.py`。
2. 新增 Session/Preset/API Config 的集成测试与 in-use 保护验证：`backend/tests/test_session_crud.py`、`backend/tests/test_preset_crud.py`、`backend/tests/test_api_config_crud.py`。
3. 前端新增 Session/Preset 类型、API 封装与 Pinia store：`frontend/src/types/session.ts`、`frontend/src/types/preset.ts`、`frontend/src/api/sessions.ts`、`frontend/src/api/presets.ts`、`frontend/src/stores/session.ts`、`frontend/src/stores/preset.ts`、`frontend/src/stores/api-error.ts`。
4. 搭建统一 Panel 架构与面板组件：`frontend/src/components/panels/PanelShell.vue`、`frontend/src/components/panels/ConfigSelector.vue`、`frontend/src/components/panels/ContentOverlay.vue`、`frontend/src/components/panels/SaveIndicator.vue`、`frontend/src/components/panels/SessionPanel.vue`、`frontend/src/components/panels/ApiConfigPanel.vue`、`frontend/src/components/panels/PresetPanel.vue`、`frontend/src/components/panels/LorePanel.vue`、`frontend/src/components/panels/AppearancePanel.vue`。
5. 引入自动保存 composable，并重构 App 容器与样式变量：`frontend/src/composables/useAutoSave.ts`、`frontend/src/App.vue`、`frontend/src/styles/variables.scss`。
6. 新增 Preset 拖拽依赖：`frontend/package.json`（`vuedraggable`）。

## M2 修补
1. 侧边栏改为图标并新增 RST Lore / Extensions / Log 入口：`frontend/src/components/panels/PanelShell.vue`、`frontend/src/components/panels/LogPanel.vue`、`frontend/src/components/panels/RstLorePanel.vue`、`frontend/src/components/panels/ExtensionsPanel.vue`。
2. 修复 Pinia store 中 `useMessage` 报错，改用离散 message：`frontend/src/utils/message.ts`、`frontend/src/stores/api-config.ts`、`frontend/src/stores/preset.ts`、`frontend/src/stores/session.ts`。
3. Lore / Appearance 面板补齐通用选择/新建/重命名/删除（暂存 localStorage）：`frontend/src/components/panels/LorePanel.vue`、`frontend/src/components/panels/AppearancePanel.vue`。
4. 修复面板点击外部误折叠（忽略 Naive UI 浮层点击）：`frontend/src/components/panels/PanelShell.vue`。
5. API 配置增强：模型选择器更宽、Temperature/Max Tokens 双输入（滑条+数字）、Max Tokens 仅整数、Temperature 步长 0.05：`frontend/src/components/api-config/ModelSelector.vue`、`frontend/src/components/panels/ApiConfigPanel.vue`。
6. 修复选中状态不同步（面板内容与下拉选项不一致）：`frontend/src/components/panels/ApiConfigPanel.vue`、`frontend/src/components/panels/PresetPanel.vue`、`frontend/src/components/panels/SessionPanel.vue`。
7. Preset 面板图标替换问号占位符：`frontend/src/components/panels/PresetPanel.vue`。
8. Overlay 改为全屏居中且支持未保存提示对话框；布局放大并调整 Comment/Disabled 位置与语义：`frontend/src/components/panels/ContentOverlay.vue`。

---

## 增量更新（2026-02-27）

1. 会话发送支持“空输入也可 Send”，并保留 Stop 取消行为：`frontend/src/views/ChatView.vue`、`frontend/src/stores/chat.ts`、`backend/app/models/chat.py`。
2. Prompt Builder 空输入组装规则落地：
- 最新 visible 消息为 user：将该消息作为 `user_input`，并避免该消息在 `chat_history` 重复注入。
- 否则：使用 `"continue"` 作为 `user_input`。
- 以上两种空输入路径都不新增会话消息。
3. 后端消息存储新增 `find_latest_visible()`，用于空输入发送时定位最近可见消息：`backend/app/storage/message_store.py`。
4. Provider 聊天返回统一升级为结构化结果（文本 + 原始请求 + 原始响应），异常也携带请求/响应上下文，便于日志完整记录：`backend/app/providers/base.py`、`backend/app/providers/openai.py`、`backend/app/providers/anthropic.py`、`backend/app/providers/gemini.py`。
5. Log 模型扩展并记录关键元数据：`provider`、`status`、`duration_ms`、`prompt_tokens`、`completion_tokens`、`total_tokens`、`stop_reason`，同时保留 `raw_request/raw_response`：`backend/app/models/log.py`、`backend/app/services/chat_service.py`。
6. Log 面板改为“元数据概览 + 完整请求响应详情”展示：`frontend/src/types/log.ts`、`frontend/src/components/panels/LogPanel.vue`。
7. 新增后端回归测试覆盖空输入发送与日志元数据提取：`backend/tests/test_chat_flow.py`。

## 验证记录（2026-02-27）

1. 前端类型检查通过：`pnpm -C frontend type-check`。
2. 前端构建通过：`pnpm -C frontend build`。
3. 后端改动文件语法编译通过：`python -m py_compile ...`。
4. 后端 pytest 在当前环境未跑通（缺少依赖 `nanoid`）。
5. 前端 vitest 现有基线失败（`naive-ui` mock 缺少 `darkTheme` 导出），与本次改动无直接耦合。

---

## 增量更新（2026-02-28）- RST Lores 模块落地

### 后端
1. 新增 Lore 数据模型与 API 模型：`backend/app/models/lore.py`。
2. 新增 Lore 存储层，覆盖 category/character/index/template/memory 的读写与重建索引：`backend/app/storage/lore_store.py`。
3. 新增 Lore 业务层，覆盖条目 CRUD、人物 CRUD、形态管理、记忆管理、模板管理：`backend/app/services/lore_service.py`。
4. 新增 NLP 检索引擎（jieba + BM25，含缺失依赖回退策略）：`backend/app/services/lore_nlp.py`。
5. 新增 Lore 调度器，支持两阶段调度、缓存候选、known_by 记忆过滤、LLM 确认生成注入块：`backend/app/services/lore_scheduler.py`。
6. 新增 Lore 动态更新器，支持从对话提取结构化更新、写入 plot/memory、触发记忆合并：`backend/app/services/lore_updater.py`。
7. 新增 Lores 路由并接入主应用：`backend/app/routers/lores.py`、`backend/app/main.py`。
8. 将 RST/ST Lore 注入与异步预检索/同步流程接入主聊天链路：`backend/app/services/chat_service.py`。
9. 扩展 Session 字段 `lore_sync_interval`，补齐校验与响应；创建会话时初始化 `rst_data/default/*.json`、`.index/index.json`、`scheduler_template.json`：`backend/app/models/session.py`、`backend/app/services/session_service.py`、`backend/app/routers/sessions.py`。
10. 后端新增依赖：`backend/pyproject.toml`（`jieba`、`rank-bm25`）。

### 前端
1. 新增 Lore 类型定义：`frontend/src/types/lore.ts`。
2. 新增 Lore API 封装：`frontend/src/api/lores.ts`。
3. 新增 Lore Store（条目/人物/调度状态/模板）：`frontend/src/stores/lore.ts`。
4. 将 `RstLorePanel` 从占位改为可用基础面板（设定条目、人物、调度器状态与模板管理）：`frontend/src/components/panels/RstLorePanel.vue`。
5. Session 面板新增 `lore_sync_interval` 配置项与自动保存链路：`frontend/src/components/panels/SessionPanel.vue`、`frontend/src/types/session.ts`。

### 测试与验证
1. 新增后端 Lores API 回归测试：`backend/tests/test_lores_api.py`。
2. 后端测试通过：`cd backend && pytest -q`（新增与既有用例全部通过）。
3. 前端类型检查通过：`cd frontend && pnpm type-check`。
4. 前端构建通过：`cd frontend && pnpm build`。

---

## 增量更新（2026-02-28）- Lore 导入逐条处理报告 Overlay

### 后端
1. 扩展导入报告模型，新增逐条处理记录 `actions`，记录每个源条目的处理动作、目标类别、创建对象、备注、警告与错误：`backend/app/models/lore.py`。
2. 转换器新增动作追踪与回填逻辑：
- 在通用条目导入、人物导入、faction 保留/拆分、失败兜底等路径记录 `ConversionAction`。
- 将 `ConversionWarning` 自动关联到对应 action，便于前端逐条展示。
- faction 拆分时记录势力摘要动作与拆分子人物动作。
3. 导入 API 响应携带完整 `actions` 数据供前端展示：`backend/app/routers/lores.py`、`backend/app/services/lore_converter.py`。

### 前端
1. 扩展导入报告类型，新增 `ConversionAction` 与 `ConversionReport.actions`：`frontend/src/types/lore.ts`。
2. `RstLorePanel` 新增导入结果 Overlay（NModal）：
- 导入成功后自动弹出报告；
- 展示每个条目的处理措施（动作类型、来源/目标类别、创建 ID、处理说明、警告、错误）；
- 保留原有导入确认弹窗与摘要消息提示。
3. 保持 API/store 透传导入报告，无额外请求：`frontend/src/api/lores.ts`、`frontend/src/stores/lore.ts`、`frontend/src/components/panels/RstLorePanel.vue`。

### 测试与验证
1. 新增并更新导入测试断言，覆盖 `actions` 字段与拆分场景：`backend/tests/test_lore_converter.py`。
2. 后端回归通过：`cd backend && pytest tests/test_lore_converter.py tests/test_lores_api.py -q`。
3. 后端全量通过：`cd backend && pytest -q`。
4. 前端测试通过：`cd frontend && pnpm test`。
5. 前端类型检查通过：`cd frontend && pnpm type-check`。

---

## 增量更新（2026-02-28）- RST Lores Panel 交互一致性与跨 Session 复制

### 后端
1. 为 Lore 条目新增重排接口数据模型 `LoreEntryReorder`，约束分类合法性并禁止 `character/memory`：`backend/app/models/lore.py`。
2. 新增条目重排业务逻辑 `reorder_entries`，校验 `entry_ids` 与分类内条目一一对应后持久化顺序并重建索引：`backend/app/services/lore_service.py`。
3. 新增路由 `PUT /sessions/{session_name}/lores/entries/reorder`，返回重排后的条目列表：`backend/app/routers/lores.py`。
4. 新增后端回归测试覆盖重排接口与顺序持久化：`backend/tests/test_lores_api.py`。

### 前端
1. Lore API 与类型新增重排能力：`frontend/src/types/lore.ts`、`frontend/src/api/lores.ts`。
2. Lore Store 接入重排请求与当前分类状态管理，保持前端列表顺序与后端一致：`frontend/src/stores/lore.ts`。
3. `RstLorePanel` 设定条目页签对齐 Preset 风格：
- 列表支持复选框、批量删除、批量复制、拖拽排序；
- 操作行 sticky 固定，条目区滚动高度自适应填充；
- 条目元数据模式显示统一为 `Const | RST`。
4. `RstLorePanel` 人物页签改为与条目同模式：
- 面板仅显示元数据与开关；
- 点击条目进入 Overlay 编辑详细内容；
- 支持复选框、批量删除、批量复制。
5. 批量复制目标统一改为“其他 Session 的 rst_data”：
- 其他设定条目：复制弹窗选择目标 Session 后写入目标 Session；
- 人物条目：复制弹窗选择目标 Session 后写入目标 Session；
- 目标列表仅展示可用 RST Session（排除当前会话与已关闭会话）。
6. 主要改动文件：`frontend/src/components/panels/RstLorePanel.vue`。

### 测试与验证
1. 后端回归通过：`cd backend && pytest tests/test_lores_api.py -q`。
2. 前端类型检查通过：`cd frontend && pnpm type-check`。
3. 前端构建通过：`cd frontend && pnpm build`。

---

## 增量更新（2026-03-02）- RST Panel 文案国际化与 Overlay 删除入口收敛

### 前端
1. `RstLorePanel` 全量接入 i18n，覆盖主面板与衍生界面文案（词条/人物/调度 Tab、批量操作确认、复制弹窗、导入弹窗、导入报告、Overlay 字段标签与提示、运行态文案、Toast 消息）：`frontend/src/components/panels/RstLorePanel.vue`。
2. 移除 RST Lores 条目与人物编辑 Overlay 底部删除入口，仅保留列表/批量删除路径，避免编辑态误删：`frontend/src/components/panels/RstLorePanel.vue`。
3. 通用 `ContentOverlay` 内置文案（Enabled/Delete/Save/Discard/Unsaved Changes/Back 等）完成 i18n 化，并将默认 content label/placeholder 改为语言包驱动：`frontend/src/components/panels/ContentOverlay.vue`。
4. 侧边栏 `PanelShell` 图标按钮文案（含 `RST Lores`、`Log` 的 `title/aria-label`）完成 i18n 化：`frontend/src/components/panels/PanelShell.vue`。
5. 新增/补齐多语言 key（`en` 与 `zh-CN`），覆盖 `rstPanel.*`、`contentOverlay.*`、`panelShell.*` 与 `common.confirm`：`frontend/src/i18n/messages.ts`。

### 测试与验证
1. 前端类型检查通过：`cd frontend && npm run type-check`。
2. 前端 lint 通过（存在仓库既有 warning，未新增错误）：`cd frontend && npm run lint`。
3. 本地脚本校验通过：`RstLorePanel/ContentOverlay/PanelShell` 中使用的 i18n key 均已在 `messages.ts` 定义。

---

## 增量更新（2026-03-02）- RST Lores 词条 Overlay 支持编辑范畴

### 后端
1. 扩展 `LoreEntryUpdate`，新增可选字段 `category`，并保持校验约束（禁止 `character/memory`）：`backend/app/models/lore.py`。
2. 调整存储层 `update_entry` 行为：当词条编辑导致范畴变化时，执行“旧范畴文件移除 + 新范畴文件追加”，避免出现条目字段与落盘文件不一致：`backend/app/storage/lore_store.py`。

### 前端
1. `RstLorePanel` 词条编辑 Overlay 新增“范畴”下拉字段（复用现有分类选项），支持新建与编辑两种场景：`frontend/src/components/panels/RstLorePanel.vue`。
2. 词条保存逻辑接入 `category` 提交：
- 新建时按 Overlay 选择范畴创建；
- 编辑时允许跨范畴修改并提交至后端。
3. 当编辑后词条范畴与当前列表筛选不一致时，前端列表自动移除该条目，保持 UI 与分类视图一致：`frontend/src/stores/lore.ts`。
4. 扩展前端类型 `LoreEntryUpdate` 支持 `category` 字段：`frontend/src/types/lore.ts`。
5. 补齐 Overlay 字段文案 i18n（`en`/`zh-CN`）：`frontend/src/i18n/messages.ts`。

### 测试与验证
1. 新增并更新后端回归断言，覆盖“编辑条目后修改范畴”的跨分类迁移场景：`backend/tests/test_lores_api.py`。
2. 后端回归通过：`pytest backend/tests/test_lores_api.py -q`（5 passed）。
3. 前端类型检查通过：`cd frontend && npm run type-check`。

---

## 增量更新（2026-03-02）- 导入链路稳定性、日志筛选与全链路超时策略统一

### 后端
1. LLM 请求日志新增来源字段 `request_source`，并在主会话链路与调度链路分别打标（`main` / `scheduler`），便于前端筛选定位：`backend/app/models/log.py`、`backend/app/services/chat_service.py`、`backend/app/services/lore_scheduler.py`、`backend/app/services/lore_updater.py`、`backend/app/services/lore_converter.py`。
2. 日志服务保留条数从 5 提升到 100，避免导入/调度过程日志被快速覆盖：`backend/app/services/log_service.py`。
3. 静态 Lore 导入的 LLM 兜底改为“失败项批处理优先 + 限额单条回退”，减少按请求次计费场景下的调用次数；新增回归测试覆盖“多人物仅一次 LLM 请求”的路径：`backend/app/services/lore_converter.py`、`backend/tests/test_lore_converter.py`。
4. Provider 调用超时参数统一集中管理并支持环境变量配置，默认值改为更适配非流式长请求的阈值（models=60s，chat=300s）：`backend/app/providers/base.py`、`backend/app/providers/openai.py`、`backend/app/providers/gemini.py`、`backend/app/providers/anthropic.py`、`.env.example`。

### 前端
1. API 客户端默认超时从 15s 调整为可配置长超时（默认 300000ms），并作为聊天/导入等长请求的统一基线：`frontend/src/api/client.ts`、`frontend/src/api/chat.ts`、`frontend/src/api/lores.ts`、`.env.example`。
2. 导入接口透传 `llm_fallback` 参数，导入弹窗增加调度 LLM 兜底开关并补齐文案：`frontend/src/api/lores.ts`、`frontend/src/components/panels/RstLorePanel.vue`、`frontend/src/i18n/messages.ts`。
3. Log 面板新增 LLM 请求来源筛选器（全部/主会话/调度器），并在列表与详情中展示来源标签：`frontend/src/components/panels/LogPanel.vue`、`frontend/src/types/log.ts`。
4. 修复导入完成提示与 RST Panel 刷新时机：
- 成功返回后先刷新 entries/characters，再弹完成消息与报告；
- 若前端超时但后端仍在导入，自动进入后台追踪刷新并给出恢复提示，降低“导入未生效”误判。
涉及文件：`frontend/src/components/panels/RstLorePanel.vue`、`frontend/src/stores/lore.ts`、`frontend/src/i18n/messages.ts`。
5. 配套调整容器/面板联动行为，保证导入后面板状态展示一致：`frontend/src/components/ContentArea.vue`、`frontend/src/components/panels/PanelShell.vue`。

### 测试与验证
1. 前端类型检查通过：`cd frontend && pnpm run type-check`。
2. 后端编译检查通过：`python -m compileall backend/app`、`python -m compileall backend/app/providers`。
3. 关键后端回归通过：
- `pytest backend/tests/test_lore_converter.py -q`
- `pytest backend/tests/test_chat_flow.py::test_logs_include_usage_and_stop_reason -q`
- `pytest backend/tests/test_lores_api.py -q`
- `pytest backend/tests/test_lores_api.py::test_schedule_route_returns_injection_block -q`
